# Only override show-name and custom icons; everything else inherits from
# the plugin's defaults.yml (full nerd-font glyph list).
config:
  show-name: false

icons:
  claude: ""   # nf-fa-robot  (also catches version-normalized names)
  copilot: ""  # nf-oct-copilot
