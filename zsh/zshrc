# Agent shells: use lightweight shell config (skip p10k, autosuggestions, etc.)
if [[ -n "$CLAUDECODE" || -n "$OPENCLAW" || -n "$GEMINI_AGENT" || -n "$PI_CODING_AGENT" ]]; then
  source ~/.dotfiles/zsh/zshrc.agent.zsh
  return
fi

typeset -g POWERLEVEL9K_INSTANT_PROMPT=quiet
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

typeset -U path PATH # keep PATH entries unique
eval "$(zoxide init zsh)" #autojump replacement
# eval "$(starship init zsh)"

export ZSH="$HOME/.oh-my-zsh"
export EDITOR="nvim"
ZSH_THEME="powerlevel10k/powerlevel10k"

fpath=(~/.zsh/completions $fpath)
plugins=(
    git 
    zsh-autosuggestions
)
source $ZSH/oh-my-zsh.sh

export GOPATH=$HOME/.local/share/go/
export PATH="$PATH:$GOPATH/bin"
bindkey -v #enable vim support
GITSTATUS_LOG_LEVEL=WARN

LFCD=~/.dotfiles/lf/lfcd.sh
if [ -f "$LFCD" ]; then
    source "$LFCD"
fi

# Load shared shell helper scripts
for f in ~/.dotfiles/zsh/scripts/*.(sh|zsh)(N); do source "$f"; done

source ~/.dotfiles/zsh/aliases.zsh
source ~/.dotfiles/zsh/keybindings.zsh

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
export FZF_CTRL_R_OPTS="--reverse"
export FZF_TMUX_OPTS="-p"
# tmuxifier
export PATH="$HOME/.tmuxifier/bin:$PATH"
eval "$(tmuxifier init -)"

# bun completions
[ -s "$HOME/.bun/_bun" ] && source "$HOME/.bun/_bun"

# bun
export BUN_INSTALL="$HOME/.bun"
export PATH="$BUN_INSTALL/bin:$PATH"

# fnm
FNM_PATH="$HOME/Library/Application Support/fnm"
if [ -d "$FNM_PATH" ]; then
  export PATH="$HOME/Library/Application Support/fnm:$PATH"
  eval "$(fnm env --use-on-cd --version-file-strategy=recursive --shell zsh)"
  # eval "$(fnm env --use-on-cd --shell zsh)"
  # eval "`fnm env`"
fi

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
export PATH="$HOME/.local/bin:$PATH"

# OpenClaw Completion
if [[ -f "$HOME/.openclaw/completions/openclaw.zsh" ]]; then
  source "$HOME/.openclaw/completions/openclaw.zsh"
fi

# Company profiles (registers jira profiles, test helpers)
EVO=~/.dotfiles/evolution/index.zsh
[ -f "$EVO" ] && source "$EVO"
ELA=~/.dotfiles/ela/index.zsh
[ -f "$ELA" ] && source "$ELA"

_jira_restore_profile

# Syntax highlighting (keep at the very end)
[ -f /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh ] && \
  source /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
